from setuptools import setup

setup(
    name='infoSpread',
    version='0.0.1',
    author='Piotr Mierzejewski',
    install_requires=[
        'requests',
        'importlib-metadata; python_version == "3.8"',
    ],
)